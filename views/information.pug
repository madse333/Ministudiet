html
    head 
        style
            include ./information.css

        script.
            async function post(url, objekt) { 
                const respons = await fetch(url, { 
                method: "POST", 
                body: JSON.stringify(objekt), 
                headers: { 
                'Content-Type': 'application/json'                         
                    } 
                }); 

                if (respons.status !== 201) // Created 
                throw new Error(respons.status); 
                return await respons.text(); 
            } 

            let betalingClickFunction = async () => { 
                try {
                    let inputs = document.querySelectorAll("input");
                    information = inputs[0].value;
                    await post("http://localhost:8080/bookInformation", {information : information});
                    window.location.href = "http://localhost:8080/";
                }
                catch (e) {
                    console.log("Fejl");
                }
            }
        
        title Information 

    body 
        div 
            table
                tr
                    td   
                        b  Fornavn: 
                    td 
                        input(id="input" type="text" placeholder="Fornavn" value="Mads")
                tr 
                    td 
                        b Efternavn: 
                    td 
                        input(type="text" placeholder="Efternavn" value="Nørskov")
                tr 
                    td 
                        b Mail: 
                    td 
                        input(type="text" placeholder="Mail" value="madsen170@yahoo.dk")
                tr
                    td  
                        b Telefonnummer: 
                    td 
                        input(type="text" placeholder="88888888" value="31393910")

        div  
            b Betalingsform 
            input(list="betalingsmetoder" value="Mobilepay")
            datalist(id="betalingsmetoder")
                option(value="Mobilepay")
                option(value="Visa")
                option(value="Mastercard")   



        div 
            b Ønsker til lokation eller andre ønsker
        div 
            textarea(name="tfxOensker", cols="30", rows="10", placeholder="Jeg kunne godt tænke mig...")
        div 
            button(id="Betaling" onclick='betalingClickFunction()') Til betaling ->

    
                    



